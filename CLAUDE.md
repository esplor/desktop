# Debian GNU/Linux desktop related settings and setup

- niri 26.04 (source in `niri/`, built with `niri-build.sh`)
- xwayland-satellite (source in `xwayland-satellite/`, built with `xwayland-satellite-build.sh`)
- mpvpaper (source in `mpvpaper/`, built with `mpvpaper-build.sh`)
- suckless tools: dwm, st, dmenu, slock, slstatus (in `suckless/`, with `patches/`)
- dell-da300/: Dell DA300 dock HDMI setup docs and diagnostic scripts
- X11/: xinitrc and touchpad config
